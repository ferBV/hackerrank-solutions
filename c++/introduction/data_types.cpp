#include <iostream>
#include <cstdio>
using namespace std;

int main() {
    // Complete the code.
    int en;
    long lon;
    char ch;
    float fl;
    double dou;
    scanf("%d %ld %c %f %lf", &en, lon, ch, fl, dou);
    printf("%d %ld %c %f %lf", en, lon, ch, fl, dou);

    return 0;
}
